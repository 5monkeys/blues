{% extends 'supervisor/default/program.conf' %}
{% block program %}
command={{ virtualenv }}/bin/gunicorn -w {{ workers }} -b {{ socket|default('0.0.0.0:8000') }} {{ module|default('django.core.handlers.wsgi:WSGIHandler()') }}
{% endblock %}