master = true
die-on-term = true

vacuum = true
no-orphans = true
log-date = true
logto = /var/log/uwsgi/%n.log

stats = /run/uwsgi/%n-stats.sock

{% for key, value in env.iteritems() %}
env = {{ key }}={{ value }}
{%- endfor %}
